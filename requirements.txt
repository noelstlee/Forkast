# --- Core Data Processing & Manipulation ---
polars>=0.19.0          # Primary data processing library for 7GB+ datasets
pyarrow>=14.0.0         # For high-performance Parquet file I/O
numpy>=1.24.0           # For numerical operations
haversine>=2.8.0        # For calculating geographic distances (Phase A3)

# --- Machine Learning & Evaluation ---
scikit-learn>=1.3.0     # For preprocessing, splitting, and evaluation metrics (Recall@k, MRR, nDCG)
xgboost>=2.0.0          # For the B1 Baseline Ranking (XGBoost) model
torch>=2.0.0            # For the B2 Sequential (LSTM) model - GPU support via CUDA
torchtext>=0.15.0       # For text/sequence utilities in LSTM

# --- Visualization Dashboard ---
dash>=2.14.0            # The main framework for interactive dashboard (Phase D)
dash-bootstrap-components>=1.5.0  # For better layout components
plotly>=5.17.0          # Core plotting engine

# --- Development & Utilities ---
jupyterlab>=4.0.0       # For prototyping, EDA, and model development notebooks
tqdm>=4.66.0            # For progress bars during long-running tasks
python-dotenv>=1.0.0    # For environment variable management
matplotlib>=3.8.0       # For plotting
seaborn>=0.13.0        # For enhanced plotting